# Database Configuration
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=revenue
DB_USERNAME=revenue
DB_PASSWORD=revenue
DB_ROOT_PASSWORD=root

# Redis Configuration
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Queue Configuration
QUEUE_CONNECTION=database

# Cache Configuration
CACHE_DRIVER=redis
SESSION_DRIVER=redis

# Application Configuration
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

# Mail Configuration (per testing locale)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@revenue.local"
MAIL_FROM_NAME="${APP_NAME}"

# FTP Configuration (opzionale per testing)
FTP_HOST=
FTP_USERNAME=
FTP_PASSWORD=
FTP_REMOTE_PATH=/
FTP_PORT=21
